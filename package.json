{
  "name": "SpencerHome",
  "version": "0.0.1",
  "private": true,
  "description": "This is my home Web",
  "devDependencies": {
    "bower": "^1.8.0",
    "http-server": "^0.9.0",
    "express": "^4.15.3"
  },
  "scripts": {
    "start": "node app.js",
    "postinstall": "bower install",
    "postupdate-deps": "bower update",
    "prestart": "npm install",
    "pretest": "npm install",
    "test": "karma start karma.conf.js"
  },
  "dependencies": {
    "mongodb": "^2.2.30"
  }
}
